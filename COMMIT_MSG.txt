Reports: revenue by collection account + date-range/venue filters

- migration 054: hotspot_purchases.collection_account_id (FK ON DELETE
  SET NULL -- when an account is deleted its attribution clears and the
  revenue rolls into "Direct / global", never lingering under a ghost).
- c2b: stamp the resolved collection_account_id on each purchase (like
  router_id), so revenue attributes accurately going forward.
- reports: revenueByAccount(days, routerId?) grouped by destination
  (bank acct / paybill / till); revenueByPlan gains an optional venue
  filter; "Lifetime" supported via a 36500-day window.
- routes: /reports/revenue-by-account?days&router; revenue-by-plan +
  revenue-by-router take the wider day cap.
- web reports: a Window (7d/30d/90d/Lifetime, default Lifetime) + Venue
  filter form; new "Revenue by collection account" table; plan/account/
  venue tables + labels now follow the selected window & venue.

Verified live: by-account endpoint returns Direct/global for legacy rows,
the FK is SET NULL, and deleting an account nulls its purchase links.
